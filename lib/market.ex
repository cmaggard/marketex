defmodule Marketex.Market do
  use ExActor.GenServer

  #defstart start_link do
    #initial_state(0)
    #with {:ok, buy_pid}  <- Marketex.Book.start_link,
         #{:ok, sell_pid} <- Marketex.Book.start_link do

      #buy_pid
    #end
  #end
end
